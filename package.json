{
  "name": "workspace-downloads",
  "displayName": "Workspace Downloads",
  "description": "Download selected workspace files to a local desktop folder.",
  "version": "0.0.1",
  "publisher": "jan-tielens",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jantielens/vscode-workspace-downloads.git"
  },
  "bugs": {
    "url": "https://github.com/jantielens/vscode-workspace-downloads/issues"
  },
  "homepage": "https://github.com/jantielens/vscode-workspace-downloads#readme",
  "engines": {
    "vscode": "^1.134.0",
    "node": ">=22 <23"
  },
  "extensionKind": [
    "ui"
  ],
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "workspace-downloads.downloadFiles",
        "title": "Download Workspace Files...",
        "category": "Workspace Downloads"
      },
      {
        "command": "workspace-downloads.downloadConfiguredFiles",
        "title": "Download Configured Files",
        "category": "Workspace Downloads"
      },
      {
        "command": "workspace-downloads.clearRememberedValues",
        "title": "Clear Remembered Download Answers",
        "category": "Workspace Downloads"
      }
    ],
    "configuration": {
      "title": "Workspace Downloads",
      "properties": {
        "workspaceDownloads.files": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "order": 1,
          "markdownDescription": "Workspace-relative files or glob patterns to download. Enter one item per nonempty line and use `/` as the path separator.\n\nExamples:\n\n- `file-1.txt` downloads that file\n- `folder/file-1.txt` downloads that file from `folder`\n- `file-1.txt` on one line and `file-2.txt` on the next downloads both files\n- `*.txt` downloads all `.txt` files\n- `file-1.txt` on one line and `*.md` on the next downloads that file and all Markdown files"
        },
        "workspaceDownloads.destination": {
          "type": "string",
          "default": "",
          "scope": "resource",
          "order": 2,
          "markdownDescription": "Optional local destination folder on this desktop client. When empty, each download asks for a folder and remembers the last choice. Do not commit a machine-specific destination to shared workspace settings."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "package:vsix": "npm run package && vsce package --no-yarn",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.134.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "eslint": "^10.5.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
